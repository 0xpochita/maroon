# Local agent rules (not shared)
/frontend/agents

# Planning docs (local only)
/docs

# Root tooling deps (husky, commitlint)
node_modules

# Secrets - never commit (rules §1)
.env
.env.*
!.env.example

# OS
.DS_Store
