{
  "name": "maroon",
  "version": "0.0.0",
  "private": true,
  "description": "Maroon monorepo root - shared git tooling (husky, commitlint)",
  "scripts": {
    "commitlint": "commitlint --edit",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7"
  }
}
